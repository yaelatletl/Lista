/* Lista.h*/
#include <math.h>
int Max=50;
int lista[Max];
int x

//recuperar sucesor
cin >> x;
if (x+1 > index) {
  cout << lista[x+1];
  
//recuperar un elemento
cin >> x;
if (x > index) {
  cout << lista[x];
} 
